# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: Crontab
templates:
  - templates/cron_job.yaml
values:
  - values.yaml
chart:
  version: "0.1.0"
  appVersion: 2.0.0
tests:
  - it: Sets the correct name
    asserts:
      - isKind:
          of: CronJob
      # - equal:
      #     path: metadata.name
      #     value: test-supermarket-uploader
